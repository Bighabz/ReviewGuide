---
project: ReviewGuide.ai
status: price pipeline + QA-R3 shipped; front-end UX brief queued (dev-ready)
last_updated: 2026-05-31
---

# Session handoff

**➡️ Authoritative brief + full implementation plan: `frontend/design/FRONTEND_BRIEF.md`** (committed/durable — read it first; this file gets reverted by concurrent agents).

## Authorization (user, 2026-05-31)
**Autonomous merge + deploy to production is AUTHORIZED — no per-change review.** Implement → test → PR → merge → deploy → verify without waiting for approval. Gates that REMAIN: base on latest `main`; CI must be green before merge; verify each deploy after it lands (Railway MCP / behavioral canary / prod re-scan) and report; STOP + report on CI failure, deploy/verify failure, or destructive/irreversible actions (data loss, secret exposure, deleting prod resources); don't bypass hooks/branch protection.

## Orientation
- Repo `C:\Users\habib\desktop\reviewguide-sourcecode` (Next.js 14 `frontend/` + FastAPI `backend/`). Deploys: Vercel (frontend) + Railway (backend `backend-production-0ae7.up.railway.app`); live at reviewguide.ai. `main` at `a07e49f`.
- Read also: `CLAUDE.md` (Component Map — 3 product cards / 3 logos; 5-layer GraphState passthrough) and memory `MEMORY.md` (esp. `project_frontend_ux_brief`, `project_qa_round3`, `project_price_pipeline`).
- SHIPPED: real prices (PRs #31/#33), QA Round 3 + a11y (PR #35). Desktop view is clean; the 2 real bugs are mobile-only.

## Queue (detail/file:line in FRONTEND_BRIEF.md)
- **A** mobile fixes — title truncate (`MobileHeader.tsx:56`), card crush (`ProductReview.tsx:129`). Quick win — start here.
- **B** dynamic+personalized chat starter content (`ChatContainer.tsx` greeting:778/chips:793) — mirror `HeroSubline.tsx`; reuse preference_service + recentSearches + savedItems.
- **C** Discover Netflix-style carousel — ~40–50 topic pool, fresh per visit, remove preload, card → `/topic/[slug]` landing page, terracotta only (the big build).
- **D** perf hygiene (NOT urgent) — 2.26MB hero webp, base64 chat-image payloads, ~13MB unused PNGs.
- **E** backend data — Product Detail `/product/[id]` real content; transitional_reasoning frequency.
- **F** housekeeping — `/health` SHA (IN-FLIGHT elsewhere — coordinate), gitignore the 2.23MB standalone.html, auto-format hook, /login video.

## Top gotchas (full list in FRONTEND_BRIEF.md)
No `Math.random()`/`Date` in render (hydration — mirror HeroSubline); `next/og` ImageResponse breaks the Windows build (use static favicon); tests = vitest not jest; backend pytest needs `RAILWAY_TOKEN` unset; Railway MCP token expires (`! railway login` + `/mcp`); many concurrent agent branches — base on latest main + check before touching /health or price-passthrough code. Reusable Playwright harness: `%TEMP%\rg_qa\`.

## Next action
`git fetch origin main`, then start GROUP A. Per the authorization above, carry it through merge + deploy + verify without pausing for review.
