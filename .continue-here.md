---
project: ReviewGuide.ai — design-blueprint rebuild + Claude Code setup
status: shipped to prod; backend-data work remains
last_updated: 2026-05-31
---

<current_state>
The terracotta design-blueprint rebuild + in-chat conversational Results flow are
SHIPPED and live on production (reviewguide.ai). All CI / Deploy-backend workflows
green through PR #28. Most recent work: rotating hero subline (#25), Discover hero
now an animated recolored WebP with transparent bg (#26/#28), Trending chips removed
(#27), and a fresh Claude Code automation setup under `.claude/` + `.mcp.json`.
Work branch: `design/blueprint-implementation` (merged to main repeatedly).
</current_state>

<completed_work>
- Full frontend redesign: terracotta-on-cream tokens, 3-face type (DM Sans / Newsreader / Instrument Serif), logo system, all screens (Discover/chat/Saved/Compare/Product-Detail/About-You). Shipped PRs #20–#28.
- In-chat flow: budget-first clarifier + budget answer chips, `transitional_reasoning` through all 5 GraphState layers, editorial blog-card renderer, prior-blog collapse.
- Logo recolored cyan→terra everywhere (video → 753KB WebP + PNGs); login + Discover hero.
- QA round-2 fixes merged: budget chips, $0 price guards, 401 silenced, terra default swatch, "You" tab.
- Claude Code setup (this session): `.claude/agents/{design-fidelity-reviewer,passthrough-checker}.md`, `.claude/skills/{verify-deploy,add-graphstate-field}/SKILL.md`, `.mcp.json` (playwright+context7), `.claude/settings.json` (.env-edit block hook). CLAUDE.md Component Map added.
</completed_work>

<remaining_work>
Priority order (mostly BACKEND now):
1. **Real prices** — cards/Saved/Detail get $0 from backend; only the misleading display was hidden. Trace affiliate price → product blocks. HIGHEST value, unblocks Product Detail + Compare.
2. **Product Detail content** — WHY/specs/HONEST-NOTES are placeholders; needs real data.
3. **transitional_reasoning frequency** — composer rarely emits it; prompt tuning.
4. **Real-device mobile QA** — never done on a physical phone.
5. **Backend /health GIT_SHA wiring** — reads "unknown"; fix so "merged=deployed" is verifiable.
6. Auto-format hook — add via `/update-config` (deferred; Windows shell + prettier presence unverified).
7. `.gitignore` the 2.2MB `frontend/design/ReviewGuide standalone.html` (committed by accident).
8. Test the new `.env`-block hook actually blocks (couldn't verify at low context).
</remaining_work>

<decisions_made>
- Terracotta `#B8543A` is primary (replaced blue `#1B4DFF`); NO blue anywhere.
- Discover hero uses the recolored intro VIDEO (user reversed the earlier "no video on Discover" call) — `DiscoverHeroLogo.tsx`.
- Category chips/popular rows seed an editable `?draft=` composer, NOT auto-send `?q=` ("ask, don't assume").
- Built Claude Code automations instead of adopting Graphify/GBrain/Obsidian (built-in memory + CLAUDE.md map already cover it).
</decisions_made>

<blockers>
- Backend price data returns $0 — needs backend investigation (affiliate price pipeline).
- Railway deploy not SHA-verifiable via /health (GIT_SHA env unwired) — use Railway MCP (now connected) or behavioral canary.
</blockers>

<context>
Frontend is essentially done; center of gravity moved to backend data + a real mobile pass.
The codebase has DUPLICATE components (3 product cards, 3 logos) — see CLAUDE.md "Component Map"
to avoid editing the wrong one. New GraphState fields must thread through 5 layers (see the
`add-graphstate-field` skill + `passthrough-checker` agent).
</context>

<next_action>
Start a fresh session on the BACKEND PRICE PIPELINE: grep where affiliate prices are fetched
(`backend/app/services/affiliate/`, `mcp_server/tools/product_*`) → how price reaches the
`products`/`product_review` ui_blocks → why it arrives as 0. Then verify with the `verify-deploy`
skill after merging.
</next_action>
