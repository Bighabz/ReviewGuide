# QA loop secrets - copy to qa/.env (GITIGNORED, never commit).
# Service-role key for the qa.* tables (project qvowmjjcotdonezdtvur). Never logged.
SUPABASE_QA_KEY=
# Absolute path to the external RingCentral sender script (Send-RcSms),
# e.g. C:\me\morning-brief\bin\ringcentral.ps1 - verified and pinned at install.
RC_SCRIPT_PATH=
# Optional bypass token for authenticated QA probes.
QA_BYPASS_TOKEN=
